# Global mapping with directory paths, remote repo addresses, and delays
global_path = ".gitsync/stage"

global_mapping = {
    '/path/to/source': {
        'repo_url': '',
        'branch': 'main',
        'email': '',
        'username': '',
        'sync_delay': 30
    }
    # Add more entries as needed
}